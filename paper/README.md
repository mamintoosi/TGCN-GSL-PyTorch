# Paper

The manuscript used for journal revision/submission is not kept in the working tree.

- Submitted revision artifacts and intermediate paper folders live in git history (see tags/commits around the September 2026 revision).
- Older paper drafts and reviewer materials are under `archive/`.
- Experiment code and result runners remain in `src/` and `run_experiments.sh`.
